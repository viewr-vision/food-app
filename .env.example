# Base URL where the offering/hero videos are hosted (GCS bucket or CDN).
# Videos are NOT bundled into the build; they are fetched from here at runtime.
# Currently a public Google Cloud Storage bucket. Swap to a CDN domain
# (e.g. https://cdn.mrfood.ai) once Cloud CDN is in front of the bucket.
VITE_MEDIA_URL=https://storage.googleapis.com/astral-hold-499011-g6-media
