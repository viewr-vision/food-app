# Where the "Get notified" form posts { email, source }. Any endpoint that accepts a JSON POST
# works (Formspree, Buttondown, a Cloudflare Worker, Google Apps Script...).
# Leave empty and the form falls back to opening an email to hello@argonrobotics.ai.
VITE_NOTIFY_ENDPOINT=
